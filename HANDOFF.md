# TEAM TOPS 인트라넷 — 세션 인계 프롬프트 (2026-07-29 작성)

> 아래 내용을 새 세션 첫 메시지로 복사-붙여넣기 하세요.

---

TEAM TOPS 인트라넷 작업을 이어서 진행합니다. 먼저 저장소의 CLAUDE.md를 읽고 시작하되, 아래 인계 내용(직전 세션 2026-07-29)이 더 최신이므로 충돌 시 이 내용을 우선하세요.

[프로젝트]
- TEAM TOPS 보험대리점 인트라넷. 메인은 `index.html` 단일 HTML 앱(약 15,000줄), 서버는 `functions/index.js`(Cloud Functions — 카톡 발송·보고서 캡처·AI 프록시)와 `google-drive-sync.gs`(Apps Script).
- 데이터: localStorage + Firestore(`tops/data`, 로그인용 `tops/auth`, 기기인증 `tops/devices`) 동기화. 파일은 Firebase Storage(+구글드라이브).
- 배포: 푸시 즉시 GitHub Actions 자동 — `firebase-deploy.yml`(호스팅+firestore/storage 규칙, 모든 푸시), `functions-deploy.yml`(functions/** 변경 시만). 현재 배포 버전 **v2026.07.29-27** (`#lver`).
- 대화·주석·커밋 메시지는 한국어. 배포용 커밋마다 `#lver` 갱신(v2026.MM.DD-n).

[작업 브랜치]
- **`claude/team-tops-intranet-handoff-s7gb6n`** — 여기서 작업·푸시. `claude/**` 푸시는 즉시 라이브 배포됨을 항상 인지할 것.

[지난 세션 작업 — 전부 커밋·푸시·배포 성공 확인됨. 다시 하지 말 것]

1. **기기인증(신규 기기 SMS)**: 팀원 첫 로그인 시 등록 연락처로 Firebase 전화 인증 → 기기 등록, 이후 인증 생략. fail-open 설계·기존 기기 자동 승인·관리자 [기기인증 관리]. 콘솔 전화 활성화 완료, 실사용 정상 확인됨. (CLAUDE.md 3.6)
2. **구성원 관리**: 삭제를 완전 삭제(코드·계정 제거)로 변경, [사용정지] 기능(버튼 순서 정보수정/사용정지/삭제, 관리자 전용 [사용정지] 탭, 5중 로그인 차단 + 사용 중 실시간 로그아웃, 해제 가능).
3. **DB 배정현황 — 인원별 DB 잔여량 표**: 이름→DB종류→지역 3단 rowspan, 배정수량/잔여수량 각각 현월·이월·교환 3칸(2단 헤더·세로 구분선·가운데 정렬·교환 빨간색). 이월=전월(구매+교환-배정) 잔여, 교환=그 달 '교환완료' 건(`doneDt`, 구 데이터 신청일 폴백). 배정은 배정종류(src) 지정 재고에서 차감, src 없는 구 데이터는 이월→교환→현월 자동 차감. 과배정 행 빨간 ⚠ 경고. 잔여 0 인원은 하단 정렬+[배정완료] 배지. 월 드롭다운 연동.
4. **DB 배정 모달**: 배정수량/배정종류(현월·이월·교환)/배정일 배치, DB종류 목록을 dbInfo(업체관리 등록분)로 동적 표시. 고객 입력칸: 흰 배경·검정 글씨, 연락처 자동 하이픈(010-6413-1743), 생년월일 자동 변환(19920728→1992년7월28일), 성별 드롭다운, 보험료 자동 만원, **녹취파일 업로드**(Storage `db_recordings/`, URL만 저장 — 함정 B 준수, 업로드 중 저장 방지).
5. **DB 정보 페이지**: 상단 [내 DB 배정현황](본인, 이번 달), 표에 녹취파일 열(오디오 플레이어 재생), 셀 nowrap 1열 정렬. 신청현황·배정현황 메뉴는 관리자 전용으로 숨김(DB 정보만 전체 공개).
6. **DB 신청 모달**: 흰 배경 + 다중 행(신청인/종류/지역/수량, 행별 금액·총합, [+ 행 추가]) — 여러 명·여러 종류 한 번에 신청. 팀원은 신청인 본인 고정.
7. **카톡 DB 배정 알림 시스템**(CLAUDE.md 3.7): 배정 저장 시 해당 팀원 카카오 '나와의 채팅'으로 항목 라벨 형식(종류/배정종류/배정일 + 성함/연락처/생년월일/성별/보험료/통화가능시간/주소) 발송, 200자 분할 최대 5통. functions `dbassign`(단건+items 묶음)/`kktest`/`setmember`, authurl state로 팀원 매핑, member 필드만 매칭(닉네임 폴백 금지)·중복 매핑 발송 보류. DB 정보 [카톡 알림 연동](관리자 전용) 모달: 팀원별 연동 링크 복사/바로 연동/승인/해제/[테스트]/[배정내역 발송]/[팀원 지정](구 계정 매핑), 지정된 인원은 드롭다운에서 제외. 미발송 시 이유 토스트. `functions-deploy.yml` 신설(의존성 설치 포함).
8. 교환신청: '교환완료' 선택 시 처리일(doneDt) 기록/해제, `window.dbExchanges` 실시간 동기화 보강(stale 덮어쓰기 방지), 교환신청 페이지 실시간 재렌더 추가.
9. 문서: CLAUDE.md 3.6/3.7 추가·브랜치 갱신, 카톡 연동 가이드 PDF 사용자 전달(주의: v-18 기준이라 메시지 예시가 옛 형식 — 요청 시 갱신).

[미해결 / 다음 확인 사항]
- 카톡 연동 가이드 PDF가 v-18 기준(메시지 예시 형식 옛것) — 사용자가 원하면 최신 형식으로 재생성.
- 구조적 한계(문서화됨, 근본 해결은 별도 서버 작업): firestore.rules는 로그인만 요구(관리자 권한 클라이언트 체크), `/api/kakao` approve 등 서버 측 관리자 검증 없음, `tops/auth`에 lpw 평문·hp 노출.
- 미성년 가설동 빠른 신청(requestMeritz)은 주민번호 미입력이라 미성년 게이트 없음(이전 세션 잔존).
- 카톡 일일보고 매일 18:30 운영 중(주말 제외) — functions 건드릴 때 회귀 주의.
- DB 신청 다중 행 개편(v-27)은 배포 직후라 실사용 피드백 미확인.

[작업 방식]
- CLAUDE.md 함정 A~E 준수: (A) 목록 핸들러는 고유 식별자(코드·이름), (B) `_lsSet`·base64는 Storage URL로, (C) claims는 `_reloadClaims`/`_persistClaims`, (D) 밝은 카드/모달엔 검정 글씨 인라인 오버라이드, (E) id 중복 스캔.
- 의미 있는 수정 후: 가드 훅(`node .claude/hooks/intranet-guard.mjs`) + `intranet-guard`·`code-reviewer` 에이전트(읽기전용) 점검, 지적 반영. 사소한 수정은 가드 훅만.
- 커밋 전: 인라인 스크립트 문법 검사, 중복 id 스캔, 핵심 로직 node 모의 실행, `#lver` 갱신. functions 수정 시 `node --check` + 배포는 자동(functions-deploy.yml — 수 분 소요, Actions 성공 확인).
- 배포·코드 수정 계열 에이전트(apps-script-deployer 등)는 사용자가 명시할 때만.
